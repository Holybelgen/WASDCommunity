@model IEnumerable<Wasd.Models.ApplicationUser>

@{
    ViewBag.Title = "ShowResults";
}

<h2>ShowResults</h2>
<ul>
    @foreach (var user in Model)
    {
        <li>@user.UserName @Html.ActionLink("Add", "AddFriend", new { id = user.Id })</li>
    }
</ul>