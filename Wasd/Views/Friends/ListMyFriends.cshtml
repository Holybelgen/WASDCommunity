@using Wasd.Models;
@model IEnumerable<Wasd.Models.ApplicationUser>

@*@if (Model != null)
{*@
    <div class="grid">
        <table cellspacing="0" width="80">
            <tr>
                <th>My Friends</th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td align="left">
                        @item.UserName
                    </td>
                </tr>
            }
        </table>
    </div>
@*}*@