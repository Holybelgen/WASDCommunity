@model IEnumerable<Wasd.Models.ApplicationUser>

@{
    ViewBag.Title = "FriendsSearchResults";
}

<h2>FriendsSearchResults</h2>

@foreach(var item in Model)
{
    <div>
        <h4>@item.UserName</h4>
        <hr />
    </div>
}